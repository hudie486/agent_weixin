/** `/周期 help` 专用长文案（与 handleHelp 简短版分离） */

import { joinWxLines } from "../util/wxRichText.js";

export function periodicHelpDetail(): string {
  return joinWxLines([
    "【周期任务 · 详细】",
    "任务元数据与脚本作业目录保存在机器人数据目录下（微信默认不展示具体路径）。",
    "删除任务会同时删除该任务的作业目录（含 run.py）。",
    "如需查看本机作业目录：发「/周期 详情 <ID> 路径」。",
    "",
    "参数 [简称 <名称>]（可选）",
    "• 创建时可指定列表里第一行显示的简短名称（最多约 24 字，非法字符会自动去掉）。",
    "• 省略则根据需求描述自动生成简短摘要。",
    "",
    "参数 [stdout_nonempty|every_run]（可选）",
    "含义：任务的 deliveryMode，决定「本轮要不要往微信发消息」。",
    "• 省略该参数时，默认为 stdout_nonempty。",
    "• stdout_nonempty — 仅当脚本本轮 stdout 有内容时才推送（trim 后为空则不推送）。适合「有变化才提醒」。",
    "• every_run — 脚本进程 exit 0 就推送；若 stdout 为空则推送一句「本轮无输出」。适合「每轮都要知道跑了」。",
    "当前命令行只支持以上两种取值；没有其他关键字可选（脚本入口默认为 run.py，改代码用 /周期 修改）。",
    "",
    "密钥 / 环境变量：脚本执行时继承机器人进程环境。可在服务器环境配置，或使用管理员命令远程写入：",
    "/环境 help — 列表、设置、删除键值（写入注入配置文件，重启后仍保留）。",
    "",
    "/周期 列表 — 每任务：⏳ 名称 · 🪪ID · ⏰ 周期与下次执行（触发任务为手动说明）；有错时多一段 ⚠️（路径已脱敏）。",
    "/周期 详情 <ID前缀或完整ID> — 完整字段与上次运行结果",
    "/周期 创建 schedule cron <分> <时> <日> <月> <周> [简称 <名称>] [stdout_nonempty|every_run] <描述> — 标准 5 段 CRON，  解释（与列表/详情「下次运行」一致）",
    "CRON 五段含义：分 · 时 · 日 · 月 · 周；常用示例：`*/15 * * * *` 每 15 分钟；`0 9 * * *` 每天 9:00；`0 0 * * 1` 每周一 0:00。",
    "/周期 创建 trigger [简称 <名称>] [stdout_nonempty|every_run] <描述> — 仅手动：/周期 运行",
    "/周期 修改 <任务ID> [补充说明] — 使用创建时的 Cursor 会话（agentChatId）在作业目录续聊改脚本",
    "/周期 删除 <任务ID>",
    "/周期 启用 <ID> · /周期 停用 <ID>",
    "/周期 运行 <ID> — 立即执行一轮（python 运行作业目录下的入口脚本）",
    "",
    "若配置了 ADMIN_USER_IDS：创建、删除、启停、运行、修改 仅管理员；列表与详情不限。",
    "示例：/周期 删除 69cb47a0-faa9-4813-8833-5d295b65a8f3",
    "示例：/周期 创建 schedule cron 0 9 * * * every_run 每日九点跑日报",
  ]);
}
